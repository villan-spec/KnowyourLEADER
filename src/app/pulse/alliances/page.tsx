import AlliancesClient from "@/components/AlliancesClient";

export default function AlliancesPage() {
    return (
        <main className="min-h-screen bg-background">
            <AlliancesClient />
        </main>
    );
}
