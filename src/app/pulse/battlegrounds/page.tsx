import BattlegroundsClient from "@/components/BattlegroundsClient";

export default function BattlegroundsPage() {
    return (
        <main className="min-h-screen bg-background">
            <BattlegroundsClient />
        </main>
    );
}
